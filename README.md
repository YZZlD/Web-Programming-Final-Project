This project is a simple reservation system with email notifications on successful reservation creation. The system validates that dates are correct and in the future.

The project will not email locally without proper email setup. Customize the nodemailer configuration within src/transporter.js for email functionality.

Dependecies include:

    -Express
    -Nodemailer
    -Events
    -Path
